# FIX-02 SQL注入加固
## 1. 基本信息
- 类型：SQL注入；修复时间：2026-08-30；对应：VULN-02。
## 2. 根因定位
- 文件：`gets.py`；函数：`_login`、`_register`；审计未发现拼接 SQL。
## 3. 修复措施
- 保持所有 `cursor.execute` 使用 `%s` 参数绑定，禁止输入拼接 SQL。
## 4. 验证
- 代码审计证据：`evidence/code_audit.txt`；未发现可复现注入点。
## 5. 残留
- 新增数据库接口必须纳入同一审计规则。
